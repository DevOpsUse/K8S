apiVersion: v1
kind: Service
metadata:
  name: devopswebapp
  namespace: default
  labels:
    app: devopswebapp
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:    
    app: devopswebapp
  type: NodePort
